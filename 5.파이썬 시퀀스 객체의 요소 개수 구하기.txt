#시퀀스 객체의 요소 개수 구하기

len(시퀀스 객체) 를 이용하면 시퀀스 개체의 요소 개수를 구할수있음.

a=[0,1,2,3,4,]
len(a) -> 5가 출력이됨.

len(range(0,10,2)) -> 5가 출력이됨.

실무에서는 range를 통한 리스트 or 튜플을 사용하기 때문에
요소의 개수를 한눈에 보기 힘듬.
따라서 len을 사용하여 요소의 개수를 바로 알수있음.

#문자열의 길이 구하기

a='Hello, World!'
len(a) -> 13이 출력됨. 공백도 포함, 한글도 가능함.